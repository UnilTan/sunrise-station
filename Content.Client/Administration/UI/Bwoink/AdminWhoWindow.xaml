<ui:FancyWindow xmlns="https://spacestation14.io"
                xmlns:ui="clr-namespace:Content.Client.UserInterface.Controls"
                VerticalExpand="True" HorizontalExpand="True"
                Title="{Loc admin-who-title}"
                SetSize="400 300">
    <PanelContainer StyleClasses="BackgroundDark">
        <BoxContainer Orientation="Vertical" Margin="8">
            <Label Text="{Loc admin-who-header}" StyleClasses="LabelHeading" HorizontalAlignment="Center" Margin="0 0 0 8"/>
            <ScrollContainer VerticalExpand="True" HorizontalExpand="True" HScrollEnabled="False">
                <BoxContainer Orientation="Vertical" Name="AdminsContainer" Access="Public" VerticalExpand="True" />
            </ScrollContainer>
            <BoxContainer Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 8 0 0">
                <Button Name="RefreshButton" Access="Public" Text="{Loc admin-who-refresh}" />
                <Control MinWidth="8" />
                <Button Name="CloseButton" Access="Public" Text="{Loc admin-who-close}" />
            </BoxContainer>
        </BoxContainer>
    </PanelContainer>
</ui:FancyWindow>